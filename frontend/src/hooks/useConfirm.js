// Shared confirm-dialog hook. Re-exported from the MakerDashboard folder
// where it was originally written so every admin/maker/community destructive
// action uses the same Crafters Market-themed modal instead of the native
// browser alert.
export { useConfirm } from "../pages/MakerDashboard/useConfirm";
