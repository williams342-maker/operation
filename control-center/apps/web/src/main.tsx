import React, { useState } from "react";
import ReactDOM from "react-dom/client";
import { QueryClient, QueryClientProvider, useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Activity, Boxes, ClipboardList, Copy, Database, Download, Eye, EyeOff, HeartPulse, KeyRound, LayoutDashboard, LogOut, ListChecks, QrCode, Search, Server, Settings, Shield, Trash2, Users, X } from "lucide-react";
import QRCode from "qrcode";
import { api, apiError, bootstrapOwner, bootstrapStatus, login, logout } from "./api";
import { Badge, Button, Card, Field, GhostButton, Select, Skeleton, Table, Toolbar } from "./ui";
import "./styles.css";

const queryClient = new QueryClient();
type Page = "overview" | "org" | "users" | "servers" | "projects" | "enrollments" | "health" | "mongo" | "tasks" | "audit";
function fmt(v?: string) { return v ? new Date(v).toLocaleString() : "-"; }
function statusTone(v?: string) { return v === "online" || v === "success" || v === "active" ? "success" : v === "revoked" || v === "failure" || v === "suspended" ? "danger" : "warning"; }
function useToast() { const [message, setMessage] = useState(""); return { message, show: (v: string) => { setMessage(v); setTimeout(() => setMessage(""), 3500); } }; }
function useForm<T extends Record<string, string>>(initial: T) { const [values, setValues] = useState(initial); const field = (key: keyof T) => ({ value: values[key], onChange: (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => setValues((v) => ({ ...v, [key]: e.target.value })) }); return { values, setValues, field }; }
function ErrorText({ error }: { error: unknown }) { return error ? <p className="text-sm text-danger">{apiError(error)}</p> : null; }
function PasswordField(props: React.InputHTMLAttributes<HTMLInputElement>) { const [visible, setVisible] = useState(false); return <div className="relative"><Field {...props} type={visible ? "text" : "password"} className={`pr-11 ${props.className || ""}`} /><button type="button" aria-label={visible ? "Hide password" : "Show password"} aria-pressed={visible} onClick={() => setVisible((value) => !value)} className="absolute inset-y-0 right-0 flex w-10 items-center justify-center text-muted hover:text-text focus:outline-none focus:ring-2 focus:ring-inset focus:ring-primary">{visible ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}</button></div>; }

function Bootstrap({ onComplete }: { onComplete: () => void }) { const f = useForm({ organizationName: "", organizationSlug: "", ownerName: "", ownerEmail: "", password: "" }); const mutation = useMutation({ mutationFn: () => bootstrapOwner(f.values), onSuccess: onComplete }); return <Centered title="Create Owner"><Field placeholder="Organization name" {...f.field("organizationName")} /><Field placeholder="Organization slug" {...f.field("organizationSlug")} /><Field placeholder="Owner name" {...f.field("ownerName")} /><Field placeholder="Owner email" {...f.field("ownerEmail")} /><PasswordField placeholder="Password" autoComplete="new-password" {...f.field("password")} /><Button disabled={mutation.isPending} onClick={() => mutation.mutate()} className="w-full">Create Owner</Button><ErrorText error={mutation.error} /></Centered>; }
function Login({ onLogin }: { onLogin: () => void }) { const f = useForm({ organizationSlug: "", email: "", password: "" }); const mutation = useMutation({ mutationFn: () => login(f.values.organizationSlug, f.values.email, f.values.password), onSuccess: onLogin }); return <Centered title="OpsWorkbench"><Field placeholder="Organization slug" {...f.field("organizationSlug")} /><Field placeholder="Email" autoComplete="username" {...f.field("email")} /><PasswordField placeholder="Password" autoComplete="current-password" {...f.field("password")} /><Button className="w-full" disabled={mutation.isPending} onClick={() => mutation.mutate()}>Sign in</Button><ErrorText error={mutation.error} /></Centered>; }
function Centered({ title, children }: React.PropsWithChildren<{ title: string }>) { return <div className="flex min-h-screen items-center justify-center p-4"><Card><div className="w-96 max-w-full space-y-3"><h1 className="text-lg font-semibold">{title}</h1>{children}</div></Card></div>; }

function Overview() { const q = useQuery({ queryKey: ["overview"], queryFn: () => api.get("/overview").then((r) => r.data), refetchInterval: 30000 }); if (q.isLoading) return <Skeleton />; const d = q.data; return <div className="grid gap-4 md:grid-cols-3"><Stat label="Servers" value={d?.serverCount} /><Stat label="Online" value={d?.onlineServers} /><Stat label="Projects" value={d?.projectCount} /><Card><h2 className="font-semibold">Recent Audit</h2><Table columns={["Action", "Result", "When"]} rows={d?.recentAudit?.map((e: any) => [e.action, <Badge tone={statusTone(e.result)}>{e.result}</Badge>, fmt(e.createdAt)])} /></Card></div>; }
function Stat({ label, value }: { label: string; value?: number }) { return <Card><div className="text-sm text-muted">{label}</div><div className="mt-2 text-3xl font-semibold">{value ?? "-"}</div></Card>; }
function OrgSettings({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const q = useQuery({ queryKey: ["org-settings"], queryFn: () => api.get("/org/settings").then((r) => r.data.organization) }); const [name, setName] = useState(""); const [timezone, setTimezone] = useState("UTC"); React.useEffect(() => { if (q.data) { setName(q.data.name); setTimezone(q.data.defaultTimezone || "UTC"); } }, [q.data]); const m = useMutation({ mutationFn: () => api.patch("/org/settings", { name, defaultTimezone: timezone, status: q.data?.status || "active", expectedUpdatedAt: q.data.updatedAt }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["org-settings"] }); toast("Organization updated"); } }); if (q.isLoading) return <Skeleton />; return <Card><h2 className="font-semibold">Organization Settings</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><Field value={name} onChange={(e) => setName(e.target.value)} /><Field value={q.data.slug} disabled /><Field value={timezone} onChange={(e) => setTimezone(e.target.value)} /><Field value={q.data.status || "active"} disabled /><div className="text-sm text-muted">Created {fmt(q.data.createdAt)}</div><div className="text-sm text-muted">Updated {fmt(q.data.updatedAt)}</div></div><Button className="mt-4" onClick={() => m.mutate()} disabled={m.isPending}>Save</Button><ErrorText error={m.error} /></Card>; }

function UsersPage({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const [search, setSearch] = useState(""); const q = useQuery({ queryKey: ["users", search], queryFn: () => api.get("/org/users", { params: { search } }).then((r) => r.data) }); const f = useForm({ email: "", name: "", role: "Viewer" }); const refresh = () => { qc.invalidateQueries({ queryKey: ["users"] }); toast("User updated"); }; const create = useMutation({ mutationFn: () => api.post("/org/users", f.values), onSuccess: (r) => { qc.invalidateQueries({ queryKey: ["users"] }); toast(`One-time password: ${r.data.oneTimePassword}`); f.setValues({ email: "", name: "", role: "Viewer" }); } }); return <Card><Header title="Users" search={search} setSearch={setSearch} /><div className="mb-4 grid gap-2 md:grid-cols-4"><Field placeholder="Email" {...f.field("email")} /><Field placeholder="Name" {...f.field("name")} /><Select {...f.field("role")}><option>Viewer</option><option>Developer</option><option>Administrator</option><option>Owner</option></Select><Button onClick={() => create.mutate()}>Invite</Button></div><ErrorText error={create.error} />{q.isLoading ? <Skeleton /> : <Table columns={["Name", "Email", "Role", "Status", "Created", "Actions"]} rows={q.data?.users?.map((u: any) => [u.name, u.email, u.role, <Badge tone={u.disabledAt ? "danger" : "success"}>{u.disabledAt ? "inactive" : "active"}</Badge>, fmt(u.createdAt), <UserActions key={u._id} user={u} onDone={refresh} />])} />}</Card>; }
function UserActions({ user, onDone }: { user: any; onDone: () => void }) { const toggle = useMutation({ mutationFn: () => api.post(`/org/users/${user._id}/${user.disabledAt ? "activate" : "deactivate"}`), onSuccess: onDone }); const revoke = useMutation({ mutationFn: () => api.post(`/org/users/${user._id}/revoke-sessions`), onSuccess: onDone }); return <div className="flex gap-2"><GhostButton onClick={() => confirm("Continue?") && toggle.mutate()}>{user.disabledAt ? "Activate" : "Deactivate"}</GhostButton><GhostButton onClick={() => revoke.mutate()}>Revoke sessions</GhostButton></div>; }

function ServersPage({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const me = useQuery({ queryKey: ["me"], queryFn: () => api.get("/me").then((r) => r.data) }); const canManage = ["Owner", "Administrator"].includes(me.data?.user?.role); const q = useQuery({ queryKey: ["servers"], queryFn: () => api.get("/servers").then((r) => r.data.servers) }); const f = useForm({ name: "", slug: "", hostname: "", allowlistedRoots: "/srv" }); const create = useMutation({ mutationFn: () => api.post("/servers", { ...f.values, slug: f.values.slug.toLowerCase(), allowlistedRoots: f.values.allowlistedRoots.split(",").map((s) => s.trim()).filter(Boolean) }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["servers"] }); toast("Server created"); f.setValues({ name: "", slug: "", hostname: "", allowlistedRoots: "/srv" }); } }); const updateSlug = useMutation({ mutationFn: ({ server, slug }: { server: any; slug: string }) => api.patch(`/servers/${server._id}`, { slug, expectedUpdatedAt: server.updatedAt }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["servers"] }); toast("Server slug updated"); } }); const remove = useMutation({ mutationFn: (id: string) => api.delete(`/servers/${id}`), onSuccess: () => { qc.invalidateQueries({ queryKey: ["servers"] }); qc.invalidateQueries({ queryKey: ["overview"] }); toast("Server deleted"); } }); const incomplete = !f.values.name.trim() || !f.values.slug.trim() || !f.values.hostname.trim(); const setSlug = (server: any) => { const value = prompt("Server slug (lowercase letters, numbers, and hyphens)", server.slug || ""); if (value !== null && value.trim()) updateSlug.mutate({ server, slug: value.trim().toLowerCase() }); }; const deleteServer = (server: any) => { if (confirm(`Delete ${server.name}? The server credentials will be revoked and it will disappear from OpsWorkbench.`)) remove.mutate(server._id); }; const columns = ["Name", "Slug", "Host", "Status", "Agent", "Heartbeat", "Metrics", ...(canManage ? ["Actions"] : [])]; const rows = q.data?.map((s: any) => [s.name, s.slug || "Not assigned", s.hostname, <Badge tone={statusTone(s.status)}>{s.status}</Badge>, s.agentVersion || "manual", fmt(s.lastHeartbeatAt), s.currentState?.metrics ? "current" : "-", ...(canManage ? [<div key={s._id} className="flex gap-1"><GhostButton onClick={() => setSlug(s)}>Set slug</GhostButton><GhostButton onClick={() => deleteServer(s)}><Trash2 className="h-4 w-4" />Delete</GhostButton></div>] : [])]); return <Card><h2 className="font-semibold">Servers</h2>{canManage && <div className="mb-4 mt-3 grid gap-2 md:grid-cols-5"><Field placeholder="Name" aria-label="Server name" {...f.field("name")} /><Field placeholder="Slug" aria-label="Server slug" {...f.field("slug")} /><Field placeholder="Hostname" aria-label="Server hostname" {...f.field("hostname")} /><Field placeholder="Allowlisted roots" aria-label="Allowlisted roots" {...f.field("allowlistedRoots")} /><Button disabled={incomplete || create.isPending} onClick={() => create.mutate()}>{create.isPending ? "Creating…" : "Create"}</Button></div>}<ErrorText error={create.error || updateSlug.error || remove.error} />{q.isLoading ? <Skeleton /> : <Table columns={columns} rows={rows} />}</Card>; }
function ProjectsPage({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const servers = useQuery({ queryKey: ["servers"], queryFn: () => api.get("/servers").then((r) => r.data.servers) }); const q = useQuery({ queryKey: ["projects"], queryFn: () => api.get("/projects").then((r) => r.data.projects) }); const f = useForm({ name: "", slug: "", primaryServerId: "", githubRepository: "", branch: "main", repoPath: "", composePath: "", serviceNames: "" }); const create = useMutation({ mutationFn: () => api.post("/projects", { ...f.values, serviceNames: f.values.serviceNames.split(",").map((s) => s.trim()).filter(Boolean), adapter: "docker-compose" }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["projects"] }); toast("Project created"); } }); return <Card><h2 className="font-semibold">Projects</h2><div className="mb-4 mt-3 grid gap-2 md:grid-cols-4"><Field placeholder="Name" {...f.field("name")} /><Field placeholder="Slug" {...f.field("slug")} /><Select {...f.field("primaryServerId")}><option value="">Server</option>{servers.data?.map((s: any) => <option key={s._id} value={s._id}>{s.name}</option>)}</Select><Field placeholder="GitHub repo" {...f.field("githubRepository")} /><Field placeholder="Branch" {...f.field("branch")} /><Field placeholder="Repo path" {...f.field("repoPath")} /><Field placeholder="Compose path" {...f.field("composePath")} /><Button onClick={() => create.mutate()}>Create</Button></div><ErrorText error={create.error} />{q.isLoading ? <Skeleton /> : <Table columns={["Name", "Slug", "Adapter", "Branch", "Paths"]} rows={q.data?.map((p: any) => [p.name, p.slug, p.adapter || "docker-compose", p.branch || "-", `${p.repoPath || "-"} / ${p.composePath || "-"}`])} />}</Card>; }
type GeneratedEnrollment = { id: string; token: string; name: string; expiresAt?: string; maxUses?: number | null };
const installCommand = (token: string) => `curl -fsSL https://opsworkbench.org/install.sh | CONTROL_CENTER=https://opsworkbench.org TOKEN=${token} sudo -E bash`;
function downloadText(filename: string, value: string) { const url = URL.createObjectURL(new Blob([value], { type: "text/plain;charset=utf-8" })); const link = document.createElement("a"); link.href = url; link.download = filename; link.click(); URL.revokeObjectURL(url); }
function EnrollmentSuccess({ generated, onClose, toast }: { generated: GeneratedEnrollment; onClose: () => void; toast: (message: string) => void }) { const [qr, setQr] = useState(""); const copy = async (value: string, label: string) => { await navigator.clipboard.writeText(value); toast(`${label} copied`); }; const showQr = async () => setQr(await QRCode.toDataURL(installCommand(generated.token), { width: 320, margin: 2, errorCorrectionLevel: "M" })); return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/80 p-4" role="dialog" aria-modal="true" aria-labelledby="generated-title"><Card><div className="w-[42rem] max-w-full space-y-4"><div className="flex items-start justify-between gap-4"><div><h2 id="generated-title" className="text-lg font-semibold">Enrollment token generated</h2><p className="text-sm text-warning">This token is shown only once. Store it securely before closing.</p></div><button aria-label="Close" onClick={onClose}><X className="h-5 w-5" /></button></div><pre className="overflow-x-auto rounded-md border border-border bg-background p-3 text-sm">{generated.token}</pre><div className="flex flex-wrap gap-2"><Button onClick={() => copy(generated.token, "Token")}><Copy className="h-4 w-4" />Copy Token</Button><GhostButton onClick={() => copy(installCommand(generated.token), "Install command")}><Copy className="h-4 w-4" />Copy Install Command</GhostButton><GhostButton onClick={() => downloadText("enrollment.env", `CONTROL_CENTER_ENROLLMENT_TOKEN=${generated.token}\n`)}><Download className="h-4 w-4" />Download enrollment.env</GhostButton><GhostButton onClick={showQr}><QrCode className="h-4 w-4" />Generate QR Code</GhostButton></div>{qr && <div className="flex justify-center rounded-md bg-white p-4"><img src={qr} alt="QR code containing the enrollment install command" className="h-72 w-72" /></div>}<div className="flex justify-end"><Button onClick={onClose}>Close permanently</Button></div></div></Card></div>; }
function EnrollmentGenerate({ onClose, onGenerated }: { onClose: () => void; onGenerated: (value: GeneratedEnrollment) => void }) { const f = useForm({ name: "", expiration: "60", maxUses: "1", description: "" }); const mutation = useMutation({ mutationFn: () => api.post("/admin/enrollment/generate", { name: f.values.name, description: f.values.description || undefined, expiresInMinutes: f.values.expiration === "never" ? null : Number(f.values.expiration), maxUses: f.values.maxUses === "unlimited" ? null : Number(f.values.maxUses) }).then((response) => response.data), onSuccess: onGenerated }); return <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/80 p-4" role="dialog" aria-modal="true"><Card><div className="w-[34rem] max-w-full space-y-4"><div className="flex justify-between"><h2 className="text-lg font-semibold">Generate Enrollment Token</h2><button aria-label="Close" onClick={onClose}><X className="h-5 w-5" /></button></div><label className="block text-sm">Friendly Name<Field className="mt-1" placeholder="Production web servers" {...f.field("name")} /></label><label className="block text-sm">Expiration<Select className="mt-1" {...f.field("expiration")}><option value="15">15 minutes</option><option value="60">1 hour</option><option value="1440">24 hours</option><option value="10080">7 days</option><option value="43200">30 days</option><option value="never">Never</option></Select></label><label className="block text-sm">Maximum Uses<Select className="mt-1" {...f.field("maxUses")}><option value="1">1</option><option value="5">5</option><option value="10">10</option><option value="unlimited">Unlimited</option></Select></label><label className="block text-sm">Optional Description<Field className="mt-1" placeholder="Purpose or deployment group" {...f.field("description")} /></label><ErrorText error={mutation.error} /><div className="flex justify-end gap-2"><GhostButton onClick={onClose}>Cancel</GhostButton><Button disabled={!f.values.name.trim() || mutation.isPending} onClick={() => mutation.mutate()}>{mutation.isPending ? "Generating…" : "Generate"}</Button></div></div></Card></div>; }
function EnrollmentsPage({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const [generateOpen, setGenerateOpen] = useState(false); const [generated, setGenerated] = useState<GeneratedEnrollment | null>(null); const [filter, setFilter] = useState("active"); const q = useQuery({ queryKey: ["admin-enrollments"], queryFn: () => api.get("/admin/enrollment").then((response) => response.data.enrollments) }); const revoke = useMutation({ mutationFn: (id: string) => api.post("/admin/enrollment/revoke", { id }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["admin-enrollments"] }); toast("Enrollment token revoked"); } }); const remove = useMutation({ mutationFn: (id: string) => api.delete(`/admin/enrollment/${id}`), onSuccess: () => { qc.invalidateQueries({ queryKey: ["admin-enrollments"] }); toast("Enrollment token deleted"); } }); const records = q.data || []; const counts = { active: records.filter((e: any) => e.status === "active").length, expired: records.filter((e: any) => ["expired", "exhausted"].includes(e.status)).length, revoked: records.filter((e: any) => e.status === "revoked").length }; const shown = records.filter((e: any) => filter === "expired" ? ["expired", "exhausted"].includes(e.status) : e.status === filter); const actions = (e: any) => <div className="flex flex-wrap gap-1"><GhostButton onClick={() => alert(`${e.name}\n${e.description || "No description"}\nUsed ${e.uses || 0}${e.maxUses ? ` of ${e.maxUses}` : " times"}`)}>View</GhostButton><GhostButton disabled title="Plaintext is only available immediately after generation">Copy</GhostButton><GhostButton disabled title="Plaintext is only available immediately after generation">Download env</GhostButton>{e.status === "active" && <GhostButton onClick={() => confirm("Revoke this token?") && revoke.mutate(e._id)}>Revoke</GhostButton>}{e.status !== "active" && <GhostButton onClick={() => confirm("Permanently delete this token record?") && remove.mutate(e._id)}><Trash2 className="h-4 w-4" />Delete</GhostButton>}</div>; return <div className="space-y-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-semibold">Enrollment</h2><p className="text-sm text-muted">Generate and manage secure agent enrollment credentials.</p></div><Button onClick={() => setGenerateOpen(true)}><KeyRound className="h-4 w-4" />Generate Enrollment Token</Button></div><div className="grid gap-3 md:grid-cols-3">{(["active", "expired", "revoked"] as const).map((status) => <button key={status} onClick={() => setFilter(status)} className={`rounded-lg border p-4 text-left ${filter === status ? "border-primary bg-primary/10" : "border-border bg-panel"}`}><div className="text-sm capitalize text-muted">{status} Tokens</div><div className="mt-1 text-2xl font-semibold">{counts[status]}</div></button>)}</div><Card>{q.isLoading ? <Skeleton /> : <Table columns={["Name", "Created", "Expires", "Uses Remaining", "Created By", "Status", "Actions"]} rows={shown.map((e: any) => [e.name, fmt(e.createdAt), e.expiresAt ? fmt(e.expiresAt) : "Never", e.usesRemaining === null ? "Unlimited" : e.usesRemaining, e.createdBy, <Badge tone={e.status === "active" ? "success" : e.status === "revoked" ? "danger" : "warning"}>{e.status}</Badge>, actions(e)])} />}</Card><ErrorText error={revoke.error || remove.error} />{generateOpen && <EnrollmentGenerate onClose={() => setGenerateOpen(false)} onGenerated={(value) => { setGenerateOpen(false); setGenerated(value); qc.invalidateQueries({ queryKey: ["admin-enrollments"] }); }} />}{generated && <EnrollmentSuccess generated={generated} toast={toast} onClose={() => setGenerated(null)} />}</div>; }
function HealthPage({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const projects = useQuery({ queryKey: ["projects"], queryFn: () => api.get("/projects").then((r) => r.data.projects) }); const q = useQuery({ queryKey: ["health"], queryFn: () => api.get("/health-checks").then((r) => r.data.healthChecks) }); const f = useForm({ projectId: "", name: "", url: "https://example.com/health", expectedStatus: "200", timeoutMs: "5000", intervalSeconds: "300" }); const create = useMutation({ mutationFn: () => api.post("/health-checks", { projectId: f.values.projectId, name: f.values.name, url: f.values.url, expectedStatus: Number(f.values.expectedStatus), timeoutMs: Number(f.values.timeoutMs), intervalSeconds: Number(f.values.intervalSeconds), enabled: true }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["health"] }); toast("Health check created"); } }); return <Card><h2 className="font-semibold">Health Checks</h2><div className="mb-4 mt-3 grid gap-2 md:grid-cols-4"><Select {...f.field("projectId")}><option value="">Project</option>{projects.data?.map((p: any) => <option key={p._id} value={p._id}>{p.name}</option>)}</Select><Field placeholder="Name" {...f.field("name")} /><Field placeholder="URL" {...f.field("url")} /><Button onClick={() => create.mutate()}>Create</Button></div><ErrorText error={create.error} /><Table columns={["Name", "URL", "Enabled", "Latest", "Latency"]} rows={q.data?.map((h: any) => [h.name, h.url, h.enabled ? "yes" : "no", h.lastResult?.success === false ? <Badge tone="danger">failed</Badge> : h.lastResult?.success ? <Badge tone="success">ok</Badge> : "-", h.lastResult?.latencyMs ?? "-"])} /></Card>; }
function MongoPage({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const projects = useQuery({ queryKey: ["projects"], queryFn: () => api.get("/projects").then((r) => r.data.projects) }); const q = useQuery({ queryKey: ["mongo"], queryFn: () => api.get("/mongo-checks").then((r) => r.data.mongoChecks) }); const f = useForm({ projectId: "", name: "", databaseNameHint: "", secretReference: "MONGO_URI" }); const create = useMutation({ mutationFn: () => api.post("/mongo-checks", { ...f.values, enabled: true }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["mongo"] }); toast("Mongo check created"); } }); return <Card><h2 className="font-semibold">Mongo Checks</h2><div className="mb-4 mt-3 grid gap-2 md:grid-cols-4"><Select {...f.field("projectId")}><option value="">Project</option>{projects.data?.map((p: any) => <option key={p._id} value={p._id}>{p.name}</option>)}</Select><Field placeholder="Name" {...f.field("name")} /><Field placeholder="Database name" {...f.field("databaseNameHint")} /><Field placeholder="Agent secret ref" {...f.field("secretReference")} /><Button onClick={() => create.mutate()}>Create</Button></div><ErrorText error={create.error} /><Table columns={["Name", "Database", "Enabled", "Latest", "Latency"]} rows={q.data?.map((m: any) => [m.name, m.databaseNameHint || "-", m.enabled ? "yes" : "no", m.lastResult?.success === false ? <Badge tone="danger">failed</Badge> : m.lastResult?.success ? <Badge tone="success">ok</Badge> : "-", m.lastResult?.latencyMs ?? "-"])} /></Card>; }
function TasksPage({ toast }: { toast: (m: string) => void }) { const qc = useQueryClient(); const [state, setState] = useState(""); const [serverId, setServerId] = useState(""); const [type, setType] = useState("collect.system"); const [selected, setSelected] = useState<string | null>(null); const servers = useQuery({ queryKey: ["servers"], queryFn: () => api.get("/servers").then((r) => r.data.servers) }); const q = useQuery({ queryKey: ["tasks", state, serverId], queryFn: () => api.get("/tasks", { params: { state: state || undefined, serverId: serverId || undefined } }).then((r) => r.data) }); const detail = useQuery({ queryKey: ["task", selected], enabled: Boolean(selected), queryFn: () => api.get(`/tasks/${selected}`).then((r) => r.data) }); const create = useMutation({ mutationFn: () => api.post("/tasks", { serverId, type, idempotencyKey: `${type}:${serverId}:${Date.now()}`, payload: { projects: [], httpHealthChecks: [], mongoChecks: [] } }), onSuccess: () => { qc.invalidateQueries({ queryKey: ["tasks"] }); toast("Task queued"); } }); const cancel = useMutation({ mutationFn: (id: string) => api.post(`/tasks/${id}/cancel`), onSuccess: () => { qc.invalidateQueries({ queryKey: ["tasks"] }); qc.invalidateQueries({ queryKey: ["task"] }); toast("Task cancelled"); } }); const retry = () => detail.data?.task && create.mutate(); const rows = q.data?.tasks?.map((t: any) => [<button className="text-left text-primary" onClick={() => setSelected(t._id)}>{t.type}</button>, <Badge tone={statusTone(t.state)}>{t.state}</Badge>, t.agentId, fmt(t.availableAt), fmt(t.expiresAt), fmt(t.completedAt), ["queued", "claimed", "running"].includes(t.state) ? <GhostButton onClick={() => cancel.mutate(t._id)}>Cancel</GhostButton> : <GhostButton onClick={() => { setServerId(t.serverId); setType(t.type); retry(); }}>Retry</GhostButton>]); return <div className="grid gap-4 xl:grid-cols-[1.2fr_0.8fr]"><Card><Toolbar><h2 className="mr-auto font-semibold">Tasks</h2><Select value={state} onChange={(e) => setState(e.target.value)}><option value="">All states</option><option>queued</option><option>claimed</option><option>running</option><option>succeeded</option><option>failed</option><option>expired</option><option>cancelled</option></Select><Select value={serverId} onChange={(e) => setServerId(e.target.value)}><option value="">Server</option>{servers.data?.map((s: any) => <option key={s._id} value={s._id}>{s.name}</option>)}</Select><Select value={type} onChange={(e) => setType(e.target.value)}><option>collect.system</option><option>inspect.docker</option><option>inspect.compose</option><option>inspect.git</option><option>check.http</option><option>check.mongo</option><option>collect.telemetry</option></Select><Button disabled={!serverId} onClick={() => create.mutate()}>Queue</Button></Toolbar><ErrorText error={create.error || cancel.error} />{q.isLoading ? <Skeleton /> : <Table columns={["Type", "State", "Agent", "Available", "Expires", "Completed", "Action"]} rows={rows} />}</Card><Card><h2 className="font-semibold">Task Detail</h2>{!selected ? <p className="mt-3 text-sm text-muted">Select a task to view its timeline and sanitized result.</p> : detail.isLoading ? <Skeleton /> : <div className="mt-3 space-y-3 text-sm"><div className="grid gap-2 md:grid-cols-2"><div><span className="text-muted">State</span><div><Badge tone={statusTone(detail.data?.task?.state)}>{detail.data?.task?.state}</Badge></div></div><div><span className="text-muted">Type</span><div>{detail.data?.task?.type}</div></div><div><span className="text-muted">Claimed</span><div>{fmt(detail.data?.task?.claimedAt)}</div></div><div><span className="text-muted">Started</span><div>{fmt(detail.data?.task?.startedAt)}</div></div><div><span className="text-muted">Completed</span><div>{fmt(detail.data?.task?.completedAt)}</div></div><div><span className="text-muted">Updated</span><div>{fmt(detail.data?.task?.updatedAt)}</div></div></div><pre className="max-h-96 overflow-auto rounded-md border border-border bg-background p-3 text-xs">{JSON.stringify(detail.data?.result?.result || detail.data?.task?.result || {}, null, 2)}</pre></div>}</Card></div>; }
function AuditPage() { const [search, setSearch] = useState(""); const q = useQuery({ queryKey: ["audit", search], queryFn: () => api.get("/org/audit", { params: { action: search || undefined } }).then((r) => r.data) }); return <Card><Header title="Audit Log" search={search} setSearch={setSearch} /><Table columns={["When", "Actor", "Action", "Target", "Result", "Correlation"]} rows={q.data?.events?.map((e: any) => [fmt(e.createdAt), e.actorType, e.action, e.targetType || "-", <Badge tone={statusTone(e.result)}>{e.result}</Badge>, e.requestId])} /></Card>; }
function Header({ title, search, setSearch }: { title: string; search: string; setSearch: (v: string) => void }) { return <Toolbar><h2 className="mr-auto font-semibold">{title}</h2><div className="flex items-center gap-2"><Search className="h-4 w-4 text-muted" /><Field placeholder="Search/filter" value={search} onChange={(e) => setSearch(e.target.value)} /></div></Toolbar>; }
function AppShell({ onLogout }: { onLogout: () => void }) { const toast = useToast(); const [page, setPage] = useState<Page>("overview"); const me = useQuery({ queryKey: ["me"], queryFn: () => api.get("/me").then((response) => response.data) }); const isAdmin = ["Owner", "Administrator"].includes(me.data?.user?.role); const nav: Array<[Page, string, any]> = [["overview", "Overview", LayoutDashboard], ["org", "Organization", Settings], ["users", "Users", Users], ["servers", "Servers", Server], ["projects", "Projects", Boxes], ["health", "Health", HeartPulse], ["mongo", "Mongo", Database], ["tasks", "Tasks", ListChecks], ["audit", "Audit", ClipboardList]]; const pageTitle = page === "enrollments" ? "Administration / Enrollment" : nav.find(([key]) => key === page)?.[1] || "Not Found"; return <div className="min-h-screen md:pl-64"><aside className="fixed inset-y-0 left-0 hidden w-64 overflow-y-auto border-r border-border bg-panel p-3 md:block"><div className="mb-4 flex items-center gap-2 px-2 font-semibold"><Activity className="h-5 w-5 text-primary" /> OpsWorkbench</div>{nav.map(([key, label, Icon]) => <button key={key} onClick={() => setPage(key)} className={`mb-1 flex w-full items-center gap-2 rounded-md px-3 py-2 text-left text-sm ${page === key ? "bg-background text-text" : "text-muted hover:bg-background"}`}><Icon className="h-4 w-4" />{label}</button>)}{isAdmin && <><div className="mb-1 mt-5 flex items-center gap-2 px-3 text-xs font-semibold uppercase tracking-wide text-muted"><Shield className="h-3.5 w-3.5" />Administration</div><button onClick={() => setPage("enrollments")} className={`mb-1 flex w-full items-center gap-2 rounded-md px-3 py-2 text-left text-sm ${page === "enrollments" ? "bg-background text-text" : "text-muted hover:bg-background"}`}><KeyRound className="h-4 w-4" />Enrollment</button></>}<button onClick={onLogout} className="mt-4 flex w-full items-center gap-2 rounded-md px-3 py-2 text-left text-sm text-muted hover:bg-background"><LogOut className="h-4 w-4" />Sign out</button></aside><main className="p-5"><div className="mb-5 flex items-center justify-between"><div><div className="text-xs text-muted">OpsWorkbench / {pageTitle}</div><h1 className="text-xl font-semibold">{pageTitle}</h1></div><div className="text-xs text-muted">Last updated {new Date().toLocaleTimeString()}</div></div>{toast.message && <div className="mb-4 rounded-md border border-primary/40 bg-primary/10 p-3 text-sm">{toast.message}</div>}<ErrorBoundary>{page === "overview" && <Overview />}{page === "org" && <OrgSettings toast={toast.show} />}{page === "users" && <UsersPage toast={toast.show} />}{page === "servers" && <ServersPage toast={toast.show} />}{page === "projects" && <ProjectsPage toast={toast.show} />}{page === "enrollments" && isAdmin && <EnrollmentsPage toast={toast.show} />}{page === "health" && <HealthPage toast={toast.show} />}{page === "mongo" && <MongoPage toast={toast.show} />}{page === "tasks" && <TasksPage toast={toast.show} />}{page === "audit" && <AuditPage />}</ErrorBoundary></main></div>; }
class ErrorBoundary extends React.Component<React.PropsWithChildren, { hasError: boolean }> { constructor(props: React.PropsWithChildren) { super(props); this.state = { hasError: false }; } static getDerivedStateFromError() { return { hasError: true }; } render() { return this.state.hasError ? <Card><h2 className="font-semibold">Something went wrong</h2><p className="text-sm text-muted">Refresh and try again.</p></Card> : this.props.children; } }
function Root() { const [authed, setAuthed] = useState(Boolean(localStorage.getItem("cc.csrf"))); const [bootstrapComplete, setBootstrapComplete] = useState(false); const status = useQuery({ queryKey: ["bootstrap-status", bootstrapComplete], queryFn: bootstrapStatus, retry: false }); const logoutMutation = useMutation({ mutationFn: logout, onSettled: () => setAuthed(false) }); if (status.isLoading) return <Centered title="OpsWorkbench"><p className="text-sm text-muted">Loading</p></Centered>; if (!authed && !bootstrapComplete && status.data?.available) return <Bootstrap onComplete={() => setBootstrapComplete(true)} />; return authed ? <AppShell onLogout={() => logoutMutation.mutate()} /> : <Login onLogin={() => setAuthed(true)} />; }
ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><QueryClientProvider client={queryClient}><Root /></QueryClientProvider></React.StrictMode>);
