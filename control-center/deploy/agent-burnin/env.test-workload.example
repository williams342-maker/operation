# Copy to .env.test-workload on the disposable target and replace values.
# Do not commit .env.test-workload.
MONGO_INITDB_ROOT_USERNAME=opsworkbench_test
MONGO_INITDB_ROOT_PASSWORD=replace-with-disposable-random-password
MONGO_INITDB_DATABASE=opsworkbench_agent_burnin
